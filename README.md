# Pygame-Fireworks
# 🎆 Pygame Firework Simulation (شبیه‌ساز آتش‌بازی)

این پروژه یک شبیه‌ساز آتش‌بازی زیبا و پیچیده است که با استفاده از کتابخانه **Pygame** در پایتون ساخته شده است. این شبیه‌سازی شامل فیزیک واقعی ذرات (گرانش و مقاومت هوا)، ستاره‌های چشمک‌زن در پس‌زمینه و انواع مختلفی از انفجارهای آتش‌بازی، از جمله یک الگوی منحصر به فرد **قلبی شکل**، می‌باشد.

---

## ✨ ویژگی‌های کلیدی

* **فیزیک ذرات پیشرفته:** شبیه‌سازی حرکت ذرات تحت تأثیر **گرانش** و **مقاومت هوا** برای جلوه‌ای واقع‌گرایانه.
* **دنباله (Trail) بصری:** استفاده از تکنیک محو کردن (Fading Surface) برای ایجاد دنباله‌ای نرم و محو از ذرات در حال حرکت.
* **پس‌زمینه پویا:** ستاره‌های پس‌زمینه که به صورت تصادفی و غیرهم‌زمان **چشمک** می‌زنند.
* **انواع انفجار:** شامل ۶ نوع انفجار مختلف و جذاب:
    * **Sphere (کروی):** پخش ذرات به صورت یکنواخت در تمام جهات.
    * **Heart (قلبی):** شکل دادن ذرات به الگوی قلب.
    * **Crackling (جرقه):** ذراتی که در طول عمر خود جرقه‌های ثانویه کوچک ایجاد می‌کنند.
    * **Shell (چندپوسته):** انفجار چند لایه با رنگ‌های متغیر.
    * **Ring (حلقه‌ای):** پخش ذرات به صورت یک دایره توخالی.
    * **Trail (دنباله‌دار/بید):** ذرات با عمر طولانی و سرعت پایین که دنباله‌ای مانند شاخه‌های درخت ایجاد می‌کنند.
* **کنترل ورودی:** پرتاب موشک به صورت تصادفی یا با کلیک ماوس.

---

## 🚀 نصب و راه‌اندازی

برای اجرای این پروژه، نیاز به نصب پایتون و کتابخانه Pygame دارید.

### پیش‌نیازها

* [**Python 3.x**](https://www.python.org/downloads/)
* **Pygame**

### دستورالعمل‌ها

1.  فایل کد پایتون (`fireworks.py` یا نامی مشابه) را دانلود کنید.
2.  ترمینال یا Command Prompt را باز کرده و با استفاده از `pip`، کتابخانه Pygame را نصب کنید:
    ```bash
    pip install pygame
    ```
3.  فایل کد را اجرا کنید:
    ```bash
    python fireworks.py
    ```

---

## 🛠️ جزئیات تکنیکی: قلب آتشین

انفجار قلبی یکی از پیچیده‌ترین جلوه‌های بصری این شبیه‌سازی است و با استفاده از **معادله پارامتری قلب** پیاده‌سازی شده است.

### پیاده‌سازی در `Rocket.explode()`

برای ایجاد شکل قلب، به جای پرتاب ذرات به صورت تصادفی، سرعت اولیه (`vx`, `vy`) هر ذره با استفاده از معادله زیر محاسبه می‌شود:

1.  **معادله قلب (Heart Curve):**
    $$x(t) = 16 \sin^3(t)$$
    $$y(t) = 13 \cos(t) - 5 \cos(2t) - 2 \cos(3t) - \cos(4t)$$

2.  **تنظیم سرعت:** در کد، خروجی $x(t)$ و $y(t)$ از این معادله، به عنوان **مولفه‌های سرعت اولیه** ذرات استفاده می‌شود (با یک فاکتور مقیاس‌دهی کوچک) تا ذرات به سمت تشکیل شکل قلب در آسمان پرتاب شوند.

    ```python
    # بخشی از کد:
    heart_x = scale * 16 * (math.sin(t) ** 3)
    heart_y = -scale * (13 * math.cos(t) - 5 * math.cos(2 * t) - 2 * math.cos(3 * t) - math.cos(4 * t)) 
    
    p.vx = heart_x * speed_factor
    p.vy = heart_y * speed_factor 
    ```

---

## ⚙️ کلاس‌ها و ماژول‌ها

| کلاس/بخش | وظیفه اصلی | ویژگی‌های منحصربه‌فرد |
| :--- | :--- | :--- |
| **`Star`** | رسم ستاره‌های پس‌زمینه | **چشمک زدن** بر اساس تابع سینوس و زمان (`math.sin`) |
| **`Particle`** | نمایش جرقه‌ها و ذرات پس از انفجار | اعمال **گرانش** و **مقاومت هوا**، ایجاد **دنباله** با ذخیره موقعیت‌های قبلی |
| **`Rocket`** | پرتاب پرتابه و مدیریت انفجار | تعیین **نوع انفجار** به صورت تصادفی، محاسبه سرعت قلبی شکل |
| **حلقه `main`** | رندرینگ | استفاده از **سطح محوکننده** برای ایجاد دنباله بصری در کل صفحه |

---

## 🤝 مشارکت

این پروژه یک شبیه‌سازی تفریحی است. در صورت تمایل، می‌توانید:

* انواع انفجار جدید اضافه کنید.
* رنگ‌ها و سرعت‌های مختلف را امتحان کنید.
* یک حالت Interactive برای کنترل موشک‌ها اضافه کنید.

کافیست پروژه را Fork کرده و Pull Request خود را ارسال کنید.
